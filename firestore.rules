rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all operations for development (remove in production)
    match /{document=**} {
      allow read, write: if true;
    }

    // Partners can read/write their own data
    // match /partners/{userId} {
    //   allow read, write: if request.auth != null && request.auth.uid == userId;
    // }

    // Allow public read for partners collection (for sign-in)
    // match /partners {
    //   allow read: if true;
    // }

    // Admins can read all partner data
    // match /partners/{userId} {
    //   allow read: if request.auth != null &&
    //     exists(/databases/$(database)/documents/admins/$(request.auth.uid));
    // }

    // Admins can read/write admin data
    // match /admins/{adminId} {
    //   allow read, write: if request.auth != null && request.auth.uid == adminId;
    // }

    // Waste requests - partners can read/write their own, admins can read all
    // match /wasteRequests/{requestId} {
    //   allow read, write: if request.auth != null &&
    //     (request.auth.uid == resource.data.partnerId ||
    //      exists(/databases/$(database)/documents/admins/$(request.auth.uid)));
    // }

    // Allow read/write for other collections (vouchers, notifications, etc.)
    // match /{document=**} {
    //   allow read, write: if request.auth != null;
    // }
  }
}